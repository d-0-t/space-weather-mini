# 05: Per-panel Compact replacing the global toggle

**What to build:** Per-panel Compact checkboxes with compress icon left of the label on Solar wind, Magnetosphere and Pinned webcams only, so chasers densify just the noisy panels. State persists per panel default off with native checkbox semantics; the legacy global key migrates (on maps to all three on) and is then removed.

**Blocked by:** 01 (needs the Dashboard panels to host the toggles)

**Status:** done

- [x] Solar wind, Magnetosphere and Pinned webcams each show an icon-led Compact checkbox in the panel header; no other panel does
- [x] Each toggle densifies only its own panel and persists across visits default off
- [x] Legacy global on migrates to all three on once, then the legacy key is deleted; legacy off or missing leaves defaults
- [x] Keyboard, screen-reader naming and focus visibility match the color-blind pill pattern with no aria-label
- [x] Typecheck, unit suite and axe audit stay green

## Comments

2026-09-16 – Implemented via TDD (red: compact.test.ts + CompactToggle.test.tsx + Home.compact.test.tsx, green, review fixes). New storage module `src/products/compact.ts` (versioned per-panel keys `sw:compact:<panel>:v1` + one-time legacy `compact-view` migration on→all three on then delete). New `CompactToggle` (`src/components/pages/home/components/CompactToggle/`: `Compact` label, compress icon left of the label, checkbox on the right via row-reverse, `btn--secondary` pill, native checkbox named by visible text, no aria-label) plus the shared `useCompactPanel` hook so the three panels share one load/save shape. Solar wind, Magnetosphere and Pinned webcams render it as their `CollapsiblePanel` adornment (right-aligned in the panel heading, never collapses the panel); dense classes `live-panel--compact` (two-per-row cards, min-content floor) and `pinned-webcams--compact` (tighter gaps, shallower stills) scope to the owning panel only. Global `Compact view` header toggle and `home--compact` flow rules removed. Verification: tsc clean, new 12 tests green (5 storage + 2 toggle + 5 composition incl. Mag/Pinned independence), adjacent suites green (SolarWind 12, Magnetosphere 11, Pinned 9, Home 7, Dashboard 12), Playwright green (home-a11y incl. axe audit in both modes, dashboard-layout 6, mobile-layout 8). Code review: Standards (no hard violations; duplicated 8-line panel shape extracted to `useCompactPanel`, SCSS pill duplication left on `btn--secondary` shared chrome) + Spec (Solar wind loading/error headings aligned to glossary lowercase, Mag/Pinned UI persistence tests added; legacy `off` also deletes the key as harmless cleanup leaving defaults off; icon DOM order input→label→icon paints icon-left via row-reverse like the Color-blind pill, screen-reader named by visible text). Docs: `coding-standards.md` button system now lists per-panel Compact pills, `index.scss`/`OvalGlow`/`webcams.scss` comments renamed off the removed global. Ready for review – not committed.

2026-09-16 – Follow-up: Compact visibly densifies at every width. Report: at some screen sizes the toggle did nothing. Loop (`e2e/compact.spec.ts`, kept as regression): Solar wind panel height before/after the toggle at 500/1000/1700px plus sibling-invariance plus Pinned at 1000px. Red as reported: 500px and 1700px showed no shrink (1000px passed) – cause: Compact only narrowed the card flex-basis breakpoint, so where roomy and compact agreed on wrapping (both 1-up narrow, both 2-up wide) nothing moved. Fix in `live-panels.scss` only (spacing, no font-size): compact grid gap 0.25rem, card padding 0.5rem + gap 0.25rem, explain margin-bottom 0.5rem – wrapping-independent, so the toggle always shrinks its own panel. Green: 4/4 e2e (Solar wind 500/1000/1700, Pinned 1000). Pinned side-by-side is out of scope here: that is ticket 06 (`06-pinned-layout-toggle.md`, still ready-for-agent), which owns the column/side-by-side toggle. Still not committed.

2026-09-16 – Second follow-up (human: 3-column Compact only re-gapped, still 2-up; wire Pinned side-by-side to Compact now). Per human direction: (1) roomy live cards stack one per row at every width (`flex: 1 1 100%`), so Compact is THE 2-up switch – 2 by side when compact, one per row when not; (2) ticket 06 folded: no separate layout toggle, Compact ON is the side-by-side (`06-pinned-layout-toggle.md` marked done/folded). Loop extended with row-count assertions (Solar roomy 4 rows → Compact 2 rows at 500/1000/1700; Pinned roomy 2 rows → Compact 1 row at 1000px with two pins). Red en route, both real: Pinned stayed 2 rows because `.pinned-webcam-card` lacked `box-sizing: border-box` (live-panel cards have it) – padding/border overflowed the 50% basis and wrapped; fixed plus a wider gap share (`calc(50% - 0.5rem)`) and a fluid 16:9 Twitch frame in row mode. Loop harness also hardened (`toHaveCount` waits: the 500px solar read had raced the feed). Green: 4/4 e2e, tsc clean, 19 unit green. Still not committed.
