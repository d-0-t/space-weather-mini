# 07: Arrange modal editing all three buckets

**What to build:** The Dashboard header Arrange dialog that edits every layout bucket without touching the live page mid-edit, so chasers own phone, laptop and wide arrangements. Tabs hold one reorder list per bucket (1-column list, 2-column A/B, 3-column A/B/C) with mouse drag plus up/down buttons and arrow-key moves; Apply persists all buckets and re-renders, Cancel and every dismiss path discard, focus returns to the trigger, and Reset-to-default is included.

**Blocked by:** 01, 04 (needs the split panels and the per-bucket column shape)

**Status:** done

- [x] Modal opens from the Dashboard header, shows all three bucket tabs, and moves panels between columns plus reorders within columns
- [x] Mouse drag, button moves and arrow keys all drive the same dialog-only order; the live Dashboard never mutates before Apply
- [x] Apply persists every bucket and re-renders; Cancel, Escape, backdrop and close discard everything
- [x] Focus returns to the Arrange trigger and the dialog meets WCAG 2.1 AA naming and focus management
- [x] Reset-to-default restores the agreed per-bucket defaults
- [x] Typecheck, unit suite and axe audit stay green

## Comments

2026-09-17 – Implemented via TDD (red: ArrangeModal.test.tsx + Home.arrange.test.tsx + e2e/arrange-modal.spec.ts, green, review fixes). New ArrangeModal (src/components/pages/home/components/ArrangeModal/) following the Time modal pattern, opened by the Dashboard header's Rearrange button (human copy decision: button and heading both "Rearrange"). The dialog's open-on-mount, backdrop-dismiss and focus-return choreography is extracted into the shared useDialogModal hook (src/components/Dialog/); TimeModal and AlertsDialog were refactored onto it, their suites staying green. It opens on the tab matching the live Layout bucket (useLayoutBucket); the three tabs follow the webcams-tablist pattern, each showing its bucket's reorder lists with panels named by their visible h2 headings ("Oval glow" rides ticket 04's open question); unknown future ids list under their raw id and are preserved. Mouse drag is native HTML5 DnD with the whole row as the handle (no separate grip, no dependency); dropping on a row takes that row's place (above it when dragging up, below when dragging down - the standard sortable model, pinned by tests in both directions), dropping on a column appends, and the row's dragOver owns its target (stopPropagation keeps the list's end-of-list fallback from overwriting the row highlight). Buttons: Move up/down plus left/right between columns on multi-column tabs (story 10's "movable by buttons"); arrow keys on focused rows move panels (Up/Down within a column, Left/Right between columns); rows carry the roving tab focus (webcams-tabs precedent) and the per-row buttons are click-only aids (tabIndex -1), arrows being the keyboard path - same-functionality rule of WCAG 2.1.1. Reset-to-default (human decision) edits the draft only; Apply commits all buckets via saveDashboardLayout and Home re-renders (layout is now state). Review fixes: shared useDialogModal extraction (third copy of dialog choreography), paired type tokens in ArrangeModal.scss (ADR-0009), drop-highlight bubbling fix, identity tab-label map removed, Home JSX indentation, downward-drag landing test added. Adjacent e2e fixes for pre-existing red on main (verified on a clean stash first): home-interpreter's combobox "Summary"→"Time" (ticket 02 rename follow-through) and smoke's "Oval glow intensity, full size"→"Oval glow, full size" (ticket 04 heading rename follow-through). Verification: tsc clean, 983 unit tests green (99 files, incl. 30 ArrangeModal + 4 composition), 102/102 Playwright green incl. the axe audit with the dialog open and real mouse-drag/arrow-key/persistence journeys. Ready for review – not committed.

Open question for the human: the dialog card chrome (fixed centered surface, actions row, close button, backdrop) is now duplicated value-for-value across arrange-dialog, time-dialog and alerts SCSS - left as-is per the btn--secondary shared-chrome precedent; extract a shared dialog chrome class if preferred.

2026-09-17 – Human-review follow-up (not committed). (1) Real estate: the card grew from fit-content under a 40rem cap to `width: min(90vw, 60rem)` with `box-sizing: border-box`, so it fills the viewport when narrow and sits at a roomy 960px on big screens. (2) Fractional columns: the 2-column and 3-column tabs now mirror the Dashboard's fractions - mobile-first, the default is a wrap-friendly flex (basis 18rem, grow 1/2/1.5 per column), and the exact grids apply at the same steps the Dashboard itself takes: `minmax(0, 1fr) minmax(0, 2fr)` from lg, `minmax(0, 1fr) minmax(0, 2fr) minmax(0, 1.5fr)` from xl. Zero floors (minmax(0, Nfr)) matter: plain `Nfr` tracks carry an auto min-content floor, and the rows' 44px touch-target buttons pushed the tracks off ratio (caught by the new computed-geometry e2e). Rows gained `flex-wrap: wrap` so their controls tuck under the label when a track is tight. New e2e: roomy-vs-viewport width, exact 1:2 / 1:2:1.5 ratios at wide viewports, wrap stacking at 500px. (3) The dialog note now explains the bucket switching: "How many columns the Dashboard shows depends on your screen or window size." (4) The two conditional panels carry a one-line note under their row label (human ask): Pinned webcams - "Only shown if you have pinned webcams." with the (pinned) "webcams" link to /webcams (the modal dismisses first, discarding the draft like every other dismiss path); Possible locations - "Only shown when the aurora is strong enough and the sky is dark enough." (the two Reach towns eligibility reasons). Both live in the CONDITIONAL_PANEL_NOTES map (a render function so the link can reach the dialog's dismiss). Adjacent human edits preserved: the Rearrange trigger's Dashboard icon and the PinnedWebcams.scss `&__img` nesting fix. Verification: tsc clean, 988 unit tests green (incl. 5 new copy/wiring tests), 105/105 Playwright green (arrange spec now 8 tests incl. axe with dialog open).

(End of file - total 14 lines)
