# 02: Display timezone setting — foundation, Time modal, Local conditions & webcams

**What to build:** The Display timezone setting itself, end to end. A two-state preference — Local (the device zone, the default) or UTC — persisted under a versioned storage entry following the house storage-module pattern, provided app-wide through a small root context so every surface updates live. A single display-time module owns all absolute-time rendering (short absolute strings, chart ticks, tooltips, slot ranges, day labels, day bucketing, Issued lines; relative age stays zone-free). The first live surface is the dashboard freshness lines. The control: a settings-gear button labeled "Time", styled like Astro mode and placed before it, opening a modal with a "Show times in UTC" checkbox plus Apply / Cancel / X / Escape handling and the explanatory note (times follow the device's timezone no matter which place is picked; the UTC-dated tables stay UTC). Local conditions times — hourly strip, luminosity timeline, updated-at — are converted from place-local times and rendered in the Display timezone (the weather contract retains the UTC offset and IANA timezone Open-Meteo already returns; the device zone is authoritative in Local mode, never the geocoded place's clock). The webcam "Loaded" stamp obeys the setting too.

**Blocked by:** None (can start immediately).

**Status:** done

- [x] Time button present before Astro mode, styled like it, reachable from the hamburger menu
- [x] Modal: checkbox unchecked by default; Apply saves and closes; Cancel, X, and backdrop/Escape discard; focus handling matches the alerts modal pattern
- [x] Choice persists across visits; all wired surfaces update live without reload
- [x] Dashboard freshness lines render in the chosen zone, with the " UTC" suffix only in UTC mode
- [x] Relative ages ("just now", "15m ago") read identically in both modes
- [x] Local conditions hourly strip, luminosity timeline, and updated-at timestamps render in the Display timezone via place-local conversion
- [x] Webcam "Loaded" stamp obeys the setting
- [x] Modal explains the device-timezone rule and mentions the UTC-dated tables exception
- [x] Display-time module unit tests pass with pinned timezone and injected clock: both modes, midnight crossing, place-local conversion with offset
- [x] Storage module tests cover load, save, default fallback, and invalid stored data

## Comments

- Implemented 2026-09-08, TDD red→green per slice. New modules: `products/display-timezone.ts` (storage, house pattern, key `sw:display-timezone:v1`), `products/display-time.ts` (the display-time module: `formatShort` with the suffix rules + midnight-crossing date, `formatClock` HH:MM labels, `formatPlaceLocal` offset-resolved conversion with strict wire-shape validation — V8 leniently parses garbage like "nope:00Z", so the naive Open-Meteo shape is regex-checked first), and `components/DisplayTimezone/DisplayTimezoneContext.tsx` (root provider; the hook throws outside the provider, matching `AlertsContext`). The Time modal mounts only while open (checkbox always starts from the stored choice; no stale-state reset needed) and follows the alerts-modal pattern: native `<dialog>`, sr-only `aria-labelledby`, focus return to the trigger on close, backdrop via pointerdown-outside-rect (FullSizeModal precedent). Wired surfaces: `FreshnessLine` (dashboard), Local conditions (hourly strip, luminosity timeline, updated-at, daily sun times), webcam Loaded stamps (image + live cards now store the instant and format at render, so a mode flip restamps live). Provider wraps added to the suites that render consumers; TZ pins added to the offline/sparkline/AuroraNow/webcam-stamp suites per the per-file convention. Suite state: typecheck clean, 718 Vitest green, Playwright mobile-layout + webcams green; the smoke home-dashboard failure (1) and the six conditions/home a11y failures reproduce on clean HEAD (pre-existing, environmental).
- Judgement calls for review: (1) The daily row's sunrise/sunset cells also convert (ticket lists only strip/timeline/updated-at; the spec's global rule — every absolute timestamp, including place-local ones — covers them, and the fix is the same one-liner). (2) `sun.ts` day anchoring is untouched: "Today's daylight chart" still windows the device-local calendar day in both modes and only the rendered band times convert, so in UTC mode a band can start at e.g. 22:00 while the static end label reads 24:00 — re-anchoring "today" to the Display timezone's calendar day is day-grouping territory (ticket 03) and worth a maintainer call. (3) `formatClock` is suffix-free in both modes: it is the HH:MM clock label (timeline bands, webcam stamps, updated-at); the " UTC" suffix lives on the short absolute strings only, matching Local-quiet/UTC-suffixed and keeping compact labels. (4) The weather contract now hard-fails on a payload missing `utc_offset_seconds`/`timezone` (the file's own philosophy: a shape change throws loudly instead of rendering blanks); the IANA `timezone` is retained per spec but currently unconsumed — only the offset drives conversion. (5) The webcam freshness templates this ticket authors now use sentence separators per the 2026-09-06 no-bullets rule ("Loaded HH:MM. Refreshes every N min."); the page-wide bullet sweep (card titles "Name · lat", attribution metas, the Alerts strip meta, the LiveBanner) is untouched and out of scope. (6) The gear icon is moon-gold so the button really is "styled like the Astro mode button". (7) The three dialogs' shared chrome (time-dialog/alerts-dialog/image-modal) is still duplicated SCSS — consolidation belongs to ticket 05. (8) "Injected clock": the module's absolute-time functions are pure (instants in, strings out — nothing to inject); the clock seam becomes real when relative-age formatting (formatAge) lands in the module with ticket 03.
- Follow-up (2026-09-08, maintainer request): the Time button's label now carries the current setting — "Time (local)" / "Time (UTC)" — updating live on Apply, and the modal test seam moved from the bare button name to the suffixed one. Tooling fact discovered en route: this dom-accessibility-api version computes a button's accessible name from its `title` attribute when one is present, so the tooltip must carry the same string as the visible label (the Astro button only ever passed because its title equals its label). Maintainer review edits folded in: the dialog is now named by its visible "Time Settings" heading, the note reads "no matter what location you've picked", and Apply is `.btn--primary`.
- Follow-up 2 (2026-09-08, maintainer request): the tooltip `title` is gone — the visible label is the button's only accessible name, so it can never disagree with it. The label also opts out of the icon-only collapse below 1000px (`button.header__time` exception in `index.scss`), so "Time (local)" / "Time (UTC)" reads as real text on mobile too; `mobile-layout.spec.ts` gained a bounding-box journey asserting the label is rendered text at 1280px and, inside the opened panel, at 390px. `docs/agents/coding-standards.md` documents the exception.
