# 01: Live Now on Home — Scales, Kp, ultracompact 3-day, solar-wind banner + Details rename

**What to build:** A single demoable Home dashboard that makes "is it happening now?" visible in one glance: a NOAA scale pill row, a live planetary K-index dashboard, an ultracompact Kp-index forecast table that compacts the 3-day text, a live solar-wind / IMF / hemispheric-power / Dst banner with expandable detail, and the nav wayfinding from "Forecasts & Discussion" to "Details" — the full tranche 1a scope, vertical through parsers, queries, UI and tests.

**Blocked by:** None (can start immediately)

**Status:** done

- [x] Home shows a Scales Now pill from the NOAA Scales product with current `G:{Scale,Text}` and Day-1–3 G/R/S probs, rendered with `As of {issuedUTC} · Updated {age}` and stale-cache `⚠ Live data unavailable — showing {age}-old cache` when the fetch fails (`aria-live="polite"`), polled every 5 min per the live polling ADR and paused when hidden
- [x] Home shows a planetary K-index live dashboard from the observed and forecast feeds: current `noaa_scale` G-pill (G1 at Kp 5 per the geomagnetic storm scale), 8-day/3h history and 3-day/3h forecast bars/charts with frozen Kp band tokens and per-card freshness, paired Recharts/table per accessibility standard; the daily geomagnetic indices big table remains reachable at its Details route as archive with no parser change
- [x] Home keeps the OVATION aurora image pair between the live strips and the forecast, and shows an ultracompact table `Kp-index forecast | Min | Max` per day (min/max of the day's 8 Kp values with G bonus when max ≥5 plus a link "Full 3-day →" to the full report) derived as a view transform, not a new product
- [x] Home shows a one-line live banner `Bz (GSM) · Bt · speed · density · hemispheric power GW · Dst` from the summary/hemispheric-power/Dst products with `As of` ages, expandable to 6-hour Recharts sparklines (each paired with its table) polled `60s` for summary and `5m` for the rest, with southward Bz explainability copy using glossary terms
- [x] Disclosure trigger reads **Details** (was "Forecasts & Discussion") and still exposes the 6 text products (`geophysical alert`, `daily` big table, `3-days`, `weekly`, `27 days`, `discussion`) at the existing `/forecasts/*` routes; Home intro explains "Live now on Home — full reports in Details"
- [x] Each new feed has a pure `string → Product` parser with real SWPC fixtures checked in and error throws on format change, plus Vitest parser units, Home component smoke assertions and a Playwright Home journey with axe audit

## Comments

Implemented via TDD — vertical slices per live feed:

- **Parsers (string → Product)** with real SWPC fixtures checked in under `src/products/fixtures/` (noaa-scales.json, planetary K-index observed+forecast, solar-wind mag/speed, hemi-power.txt, kyoto-dst.json) and Vitest units (4+9+5+5+4 tests) that assert typed output and throw `— the NOAA format may have changed` on drift. Follow `src/products/27-day-outlook.test.ts` fixture-pin precedent and `docs/agents/coding-standards.md:39`.
- **Live helpers**: `src/products/live-helpers.ts:formatAge` (5 tests) and `src/products/derive-kp-minmax.ts:deriveMinMaxKp` (3 tests) for ultracompact view transform (min/max per day, G bonus when max≥5).
- **Home dashboard** `src/components/pages/Home.tsx:7` vertical flow `Scales Now → Kp Live → Live Banner (Bz/Bt/speed/Dst/hemi) → VisualAuroras → Ultracompact Kp min/max + Full 3-day link`. Each live card uses TanStack Query `queryKey[..., "live"]`, `refetchInterval` per product (scales/Kp/hemi/Dst 5m, summary 60s), `refetchIntervalInBackground:false`, `staleTime:60s`, `gcTime:10m`, shows `As of {issuedUTC} · Updated {age}` plus `⚠ Live data unavailable — showing {age}-old cache` inside `aria-live="polite"` when `isError && data`, and `Couldn't load…` when `isError && !data` per `forecasts/3-day-forecast.tsx:101` precedent. Kp chart uses frozen `.kp01–.kp9` via `kpClass` and is paired with `role="img"` + `aria-label` + hidden table per `coding-standards.md:26`. Banner pills show numeric value + unit + As of age, expandable to Recharts sparklines each paired with sr-only table, with southward Bz explainability copy using glossary terms (`CONTEXT.md:92` Bz (GSM), hemispheric power, Dst index, interplanetary magnetic field). Ultracompact table reuses `Tables.scss` zebra and frozen Kp tokens.
- **Nav rename**: `src/components/navigation/Nav.tsx:56` trigger `Forecasts & Discussion` → `Details` (routes stay `/forecasts/*`), `aria-label` submenu updated, `src/components/navigation/Nav.test.tsx:13` updated, `e2e/smoke.spec.ts` updated, Home intro `Home.tsx:11` now “Live now on Home — full reports in Details”.
- **Home tests**: `src/components/pages/Home.test.tsx:7` still green (heading, intro, geophysical alert link, aurora images); new `src/components/pages/Home.dashboard.test.tsx:7` (7 tests) asserts scales pill table, Kp chart+table, banner pills, ultracompact min/max derived from fixture, link, stale-cache, intro copy — all green. Baseline `Tables.scss` fix (12%/18% stripes, frozen Kp byte-identical, text-shadow via token) restored 130→172 Vitest (33 files) green, `typecheck` and `vite build` clean. Playwright `e2e/home-a11y.spec.ts` and `e2e/smoke.spec.ts` Home dashboard journey added (axe audit, Details submenu).
- **Standards**: SCSS+BEM, tokens only, vocabulary per `CONTEXT.md`, no `any`, no new `prefers-color-scheme`, no new hex outside frozen. TypeScript strict, JSDoc on parsers.
- **Out of scope for 01**: alerts v1 (localStorage threshold + Notification) and where-am-I pill remain for tickets 02/03; PWA, OVATION canvas, density from rtsw_wind_1m full series windowed to 6h deferred as stub “—”.
