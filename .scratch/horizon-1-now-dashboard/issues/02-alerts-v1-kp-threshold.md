# 02: Alerts v1 — Kp threshold in localStorage + in-app banner + opt-in browser Notification

**What to build:** A chaser-set Kp threshold that drives a foreground alerts banner on Home: a slider persists in `localStorage`, the app polls the alerts feed and the live scales/forecast, shows matching watches/warnings in the banner, and offers an explicit "Enable browser alerts" tap for system notifications while remaining honest about "alerts while this tab is open".

**Blocked by:** 01 (needs the live Scales/Kp/Dst vocabulary, freshness pattern, and Home dashboard slots that 01 establishes)

**Status:** done

- [x] Home exposes a Kp threshold control (range 1–9, default 5 = G1) persisted as versioned `localStorage` (`{kp}`) and hydrates on mount; G bonus labels are shown when the threshold maps to G1–G5
- [x] Home polls the alerts feed every 5 min, filters to `WATCH`/`WARNING`/`ALERT` where `G` or forecast `kp` meets the threshold (including the next-24h forecast window), dedupes by `product_id|issue_datetime` (cap 200), and renders up to 3 newest matches as an in-app banner even without Notification permission
- [x] An explicit button "Enable browser alerts" calls `Notification.requestPermission()` on user gesture and, if granted, fires system notifications for new matches while the tab is open; copy is honest about foreground-only and iOS Home-Screen prerequisite, with `aria-live="polite"` and a stale-cache advisory when the feed is unreachable

## Comments

Implemented via TDD — vertical slices: alerts parser → threshold storage → Alerts panel → Home wiring → Playwright journey.

- **Parser seam** `src/products/alerts.ts`: `parseAlerts` (`string → SwpcAlert[]`) with the real SWPC feed checked in as `src/products/fixtures/alerts.json` (fetched 2026-08-28, `CORS:*`/`max-age=60` re-verified) plus the `alerts-empty.json` edge fixture. Each alert derives `code` (from the `Space Weather Message Code:` line), `kind` (code prefix ALT/WAR/WAT/SUM → ALERT/WARNING/WATCH/SUMMARY), `kp` (ALTK/WARK codes) and `gScale` (highest `G1–G5` mentioned in the message) — format-changed throws mirror the existing parsers. Helpers: `alertKey`, `alertMatchesThreshold`, `matchingAlerts` (filter + dedup + newest-first), `alertSnippet`, `alertTitle`, `forecastBreachInNext24h` (window `(now, now+24h]`), `newestAlertTime`, seen-set `loadSeenAlertKeys`/`saveSeenAlertKeys` (cap 200). 17 Vitest units, literals taken from the live feed shapes.
- **Threshold seam** `src/products/thresholds.ts`: `loadKpThreshold`/`saveKpThreshold` on `localStorage["sw:thresholds:v1"]` `{kp, v:1}` (default 5, clamped 1–9, corrupt/foreign-version → default), `gLabelForThreshold` (G1 at Kp 5…G5 at Kp 9), shared `gScaleForKp` (the `kp - 4` mapping used by the parser seam and the panel — one source). 6 units.
- **Alerts panel** `src/components/pages/home/components/Alerts/`: slider (hydrate/persist, `aria-valuetext` "Kp 5 (G1)"), banner filtered from the alerts feed + current NOAA scale + next-24h forecast (up to 3 newest matches, message snippet + `As of {time} · Updated {age}`, honest empty, `aria-live="polite"` on banner and stale advisory), 5-min polling with `refetchIntervalInBackground:false` per ADR 0003, `⚠ Live data unavailable – showing {age}-old cache` from the cached feed's newest issue time when any live leg fails, plain error when never loaded. "Enable browser alerts" button calls `Notification.requestPermission()` on tap (granted/denied/unsupported states), fires `showNotification` via `navigator.serviceWorker.ready` with `new Notification` fallback and an iOS `TypeError` catch; honest footnote. 13 component tests (fetch mocked at the URL boundary, `Notification` mocked at the API boundary).
- **Home wiring**: `Home.tsx` renders `<Alerts />` under the two-column flow; `Home.dashboard.test.tsx` serves the alerts fixture and asserts the banner (Home seam per spec); `e2e/alerts-a11y.spec.ts` routes `alerts.json` to the fixture, sets `sw:thresholds:v1`, asserts 3 newest matches + axe audit green.
- **Deliberate deviations from the spec's loose filter wording** (spec §Alerts v1 Q9): (1) the K-index is read from the message code (`ALTK/WARKnn`) because "message contains `G{threshold-4}`" misses K-index alerts below G1 — an `ALTK04` carries no G line, so threshold 4 would never match without the code branch; (2) G matching is `gScale >= threshold-g` (a G2 watch matches threshold G1) rather than literal substring; (3) the seen set holds synthetic keys (`scales:current:G1`, `forecast:{time_tag}`) beside `product_id|issue_datetime` so scales/forecast matches are not re-notified every poll; (4) matches enter the seen set when *notified*, so opting in surfaces currently-active alerts once and later polls stay quiet; (5) the "full alerts archive" link is deferred (no archive route exists — spec marks it future).
- **Review pass** (two-axis code review): snippet rendered in banner items (spec required), stale-cache age sourced from the cached feed (not a future forecast point), Kp→G mapping deduped into `thresholds.gScaleForKp`, `gScaleOf` moved to `noaa-scales.ts` (its product module), epoch-aware match sorting (`live-helpers.toEpoch`, also used by `formatAge`), tautological `alertKey` test replaced with a literal expectation.
- **Verification**: 293 Vitest green (+23 new), `typecheck` and `vite build` clean, Playwright alerts + home journeys pass with axe. The 5 remaining red unit tests and the `smoke.spec.ts` keyboard journey (Nav focus, severity band colors, palette sweep) are pre-existing on `main` (verified by stashing) and belong to the ui-palette-recolor effort — not this ticket.
- **Out of scope for 02**: where-am-I pill remains ticket 03; PWA/offline notifications and push remain Horizon 2 per the spec.

### Revision (chaser review of the first cut)

- **Alerts moved into Aurora Now**: the alerts block now renders inside the `AuroraNow` panel (between the Kp bar and the "Aurora Oval Forecast" heading) instead of a separate section under the two-column flow; `Home.tsx` renders it no longer.
- **Single strip**: the banner shows only the newest match (one strip), not up to 3; `match` is now a single `AlertMatch` and notifications fire for that one match only (seen-set dedup unchanged).
- **Kp-colored dot**: the strip has no background — a 1rem circle colored with the frozen `.kp01–.kp9` band class for the match's Kp level (`kpClass` in `kp-panel.tsx`, ceil-based, shared with the Forecast min/max table cells).
- **Nicer date**: `formatUtcShort` (`live-helpers.ts`) renders "As of Aug 26 16:36 UTC" instead of the raw SWPC timestamp.
- **Honest copy**: footnote shortened to "Alerts while this tab is open." — the iOS Home Screen promise is dropped (not verifiable in-app).
- Verification: 299 Vitest green (+2 `formatUtcShort`, +2 `kpClass`), typecheck + build clean, Playwright alerts/home journeys pass with axe; the 5 red unit tests remain the pre-existing ui-palette/Nav set.

### Revision (collapsible-panel survival)

- **Alerts keep working with the Aurora Now panel closed.** Collapsing the panel unmounts its body, which would have killed the alerts polling and the notification effect (no observers → no `refetchInterval`). All state now lives in an `AlertsProvider` (new `AlertsContext.tsx`) mounted in `Home.tsx` around `AuroraNow` — outside the collapsible — and `Alerts.tsx` is a pure UI consumer via `useAlerts()`. Polling and browser notifications therefore continue for the whole Home session while the tab is open; reopening the panel just remounts the strip with the current match.
- Test: "keeps polling and notifying while the strip is unmounted (panel closed)" renders the provider without the strip and asserts a notification fires and the `["alerts","live"]` query keeps its 5-min `refetchInterval`.
- `AuroraNow.test.tsx` now renders `AuroraNow` inside the provider; its h3 copy assertion follows the "Aurora Oval Forecast (30 min)" rename from the reviewer's staged edit.
- Verification: 300 Vitest green (same 5 pre-existing ui-palette/Nav failures), typecheck + build clean, Playwright alerts/home journeys pass with axe.