# 04: Full page composition with polar copy and external links

**What to build:** the daylight from ticket 01 and the weather from ticket 03 wired to the same geocoded place from ticket 02, with short polar day and polar night copy, concise local time labels for today and tomorrow and two external links baked with the current lat and lon. The 24 hour strip side scrolls and the timestamp and Refresh from ticket 03 stay.

**Blocked by:** 02, 03

**Status:** done

- [x] Daylight and weather both follow the current geocoded place, today and tomorrow are shown together and the dark window for tomorrow is visible even when today is still light; times are labelled concisely in the place's local time
- [x] Polar day renders "Sun does not set today" and polar night renders "Sun does not rise today" with the relevant twilight note, not a blank or an invalid date, pinned with a June at 69 N and a December at 69 N fixture
- [x] A card with two external links is baked with the current lat and lon: "See light pollution at this spot on lightpollutionmap.info" with zoom and centre and a B0 layer preset, and "See live cloud cover on weather-radar-live.com" with the pin, each opening in a new tab with rel noopener noreferrer; no geographic map widget or Bortle number is fetched or stored
- [x] The 24 hour hourly strip side scrolls without paging or pagination and the daily row stays at three cards; the Refresh button remains always enabled with the fetched at timestamp

## Comments

- **2026-09-01 – implemented (review pending).** Full page composition now composes the three prior seams (place store + geocoding, daylight via suncalc, weather via Open-Meteo + WMO map) behind one page seam (`src/components/pages/conditions/conditions.tsx:20`). Today + Tomorrow both render (`DayBlock` headings "Today's daylight chart" / "Tomorrow's daylight chart"), each timeline sums to 1440 min and the Night/dark window for tomorrow stays visible. Daylight remains device-local with explicit "in your time zone" label (place has no timezone field; device `Intl.DateTimeFormat` keeps times honest – spec US12 wants place-local, but model has no tz, see ticket 01 timezone note). Weather hourly/daily times are place-local verbatim from `timezone=auto`. Polar copy pins both days: June at 69 N midnight-sun shows two "Sun does not set today" notes and single Day band per day; December polar-night shows two "Sun does not rise today" and the 7-band twilight chain per day. External links are pure functions (`src/data/external-links.ts:1`) – `lightPollutionMapUrl` builds `https://www.lightpollutionmap.info/#zoom=15&lat=&lon=&layers=B0FFFFFFTFFFFFFFFFF` and `cloudCoverMapUrl` builds `https://www.weather-radar-live.com/cloud-cover-map/#zoom=8&lat=&lon=`, each opened `target=_blank rel=noopener noreferrer` via `src/components/pages/conditions/components/ExternalLinks/ExternalLinks.tsx:1` (links verified to contain current lat/lon, zoom/centre/B0/pin; no iframe/canvas/Bortle is rendered). The 24 h strip stays `role=list` with `tabIndex=0` (scrollable-region-focusable) and the daily row stays a 3-card table with caption. `conditions.test.tsx` was refactored from global `getAllByRole(listitem)` to `bandsInDay(heading)` so ticket 01 assertions scope to Today while ticket 04 pins both days and the links. New coverage: `src/data/external-links.test.ts:1` (4 pure URL tests) + 8 page-composition tests in `conditions.test.tsx`. No new backend, no polling, no map widget – ADR 0001/0003/0005 discipline kept. Typecheck clean, 530 Vitest green (was 518, +12).

- **Documentation notes.** The `ExternalLinks` BEM block reuses `:root` tokens only (`--color-border-muted-transparent`, `--gradient-card`, `--color-accent` etc.); tag nesting `h2/p/ul/a` inside `&__external` is the established local pattern (mirrors `DayBlock.scss`). The page now has one `h1` and five `h2` landmarks (Find a place, Today, Tomorrow, Weather, External maps) – axe expects 0 violations for that hierarchy. Narrow-viewport overflow is not introduced: timelines already clamp to 00:00–24:00 and use flexGrow ratios; the external links list is plain text and wraps. If a reviewer wants place-local daylight times, the next step is to persist `timezone` from the Open-Meteo payload (or from Nominatim `timezone` if added) and format with `Intl.DateTimeFormat({timeZone})` – not done here to keep the seam honest.
