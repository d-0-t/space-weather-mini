# 04: Canvas oval map (default palette)

**What to build:** A `<canvas>` oval on Home that paints the real OVATION grid over a few Stadia dark tiles, with `North | South` toggle and `Forecast Time`.

**Blocked by:** 03

**Status:** done

- [x] Fetch `ovation_aurora_latest.json` via TanStack Query `live:true` 5 min polling, `refetchIntervalInBackground:false`; decimate 1° grid, equirectangular project to `<canvas>` over Stadia `alidade_smooth_dark` tiles (locked `minZoom 1` / `maxZoom 2`, horizontal wrap, ~4 tiles north + 4 south, `CacheFirst` 7 days, attribution `© Stadia Maps © OpenMapTiles © OpenStreetMap`, no geocoding/routing)
- [x] `Aurora 0` transparent, `1-5/6-10/11-15/16+` mapped to frozen Kp palette via `color-mix` (no new hex), default shows colour wash only (no hatch, clean), `North | South` toggle drives which grid is painted
- [x] Header shows `Forecast Time` (not `Observation Time`) with `30–90 min lead` and `As of • Updated {age}` plus `⚠` stale branch; hemispheric power GW headline kept
- [x] A11y: canvas `role="img"` `aria-label` lists bands, hidden `<table>` is source of truth per coding standards, legend row shows swatch per band (no numbers on map), focus on toggle visible in Light Lime per ADR-0002
- [x] Component test: mocks Stadia tiles, asserts canvas `role` + `aria-label`, hidden table present, legend swatches present, toggle switches north/south, no hatch in default; build check asserts `vite.config.ts` Stadia `runtimeCaching` URL pattern

## Comments

- Implemented 2026-09-04 via TDD (red: missing `OvalMap`, then green slices), then two-axis review. Suite green: `tsc --noEmit`, 602 Vitest (66 files, 11 new + 1 integration), `vite build` (+ PWA `sw.js`).
- **OvalMap** – `src/components/pages/home/components/AuroraNow/OvalMap.tsx`: `fetchOvation` (URL-boundary, `OVATION_URL` + `parseOvation`), TanStack `[...OVATION_QUERY_KEY]` 5 min / background-false / 60 s stale, `hemisphere` state (default north), equirectangular `projectOvalCell` (lon wrap, north `lat>=0` / south `lat<0`), `countOvalBands` per painted hemisphere, canvas 720x180 (2px per 1-degree cell) with `Aurora 0` skipped, `North | South` buttons `aria-pressed` in a `role=group` named by visible text, `Forecast Time` + `30–90 min lead` + `FreshnessLine` (`As of` = Forecast Time, `Updated` age = Observation Time so the future valid time never inverts the age) + `StaleDataNotice`, legend swatches reuse frozen `kp34/kp45/kp67/kp89` classes, `sr-only` table with per-band counts, honest `Forecast, not a photo` note, Stadia/OSM attribution. Wired into `AuroraNow.tsx` below the JPG pair (additive – JPGs stay, e2e tile-count untouched).
- **Honest deviations (documented in code)** – tiles are fixed world-zoom-1 URLs (2 per hemisphere, 4 total – fewer than the ticket's ~4+4 estimate, keeping tile cost near zero; the fixed URLs *are* the zoom lock, wrap via lon normalization, no Leaflet/geocoding/routing). Canvas `fillStyle` reuses the exact frozen `rgb()` values (`Tables.scss:66-84`) verbatim – a `<canvas>` cannot reference a CSS class or `color-mix`, and the `.scss` contract only scans stylesheets, so no new hex/palette is introduced. Canvas `aria-label` is the `coding-standards.md:39` exception (void element cannot hold `.sr-only` + `aria-labelledby`) and the ticket/ADR-mandated shape. Toggle focus is Light Lime via the global `button:focus-visible` token (`index.scss`), no per-component rule needed.
- **Review fixes** – Standards: `img` BEM (`oval-map__tile`, no bare-element nesting), single `OVAL_BANDS` table sources canvas name + legend + hidden table (no drift), `cellInHemisphere` helper removes the repeated north/south switches. Spec: canvas now overlays the tiles (`.oval-map__stage` relative, tiles absolute behind, canvas in-flow on top over transparent cells), `Updated` age comes from `observationTime` (was future `forecastTime` → always "just now"), new build-check test asserts every `ovalTileUrls` URL matches the `stadia` `CacheFirst` runtime pattern in `vite.config.ts`.
- **Test seams** – `OvalMap.test.tsx` (11: fetch-once URL, canvas name lists bands + hemisphere + 0-note, Forecast Time + lead + As of, hidden table counts, legend frozen swatches + no hatch, toggle flips + relabels canvas, stage overlay + tiles + attribution + no Nominatim, honest explainer copy, offline stale keeps canvas, never-loaded error, Stadia CacheFirst build check); `AuroraNow.test.tsx` (+1: Oval grid embedded with Forecast Time; stale assertion widened to `getAllByText` since Kp + Oval both go stale).
- **Awaiting human review – not committed.** Working tree holds new `OvalMap.{tsx,scss,test.tsx}`, edited `AuroraNow.{tsx,test.tsx}`, and this file.

## Comments (2026-09-04 follow-up, user review round)

- **Renamed to Oval glow intensity** – `OvalMap.{tsx,scss,test.tsx}` became `OvalGlow.{tsx,scss,test.tsx}` (`AuroraNow.tsx` import follows). Heading is now `Oval glow intensity`: Oval is the glossary term, glow says brightness-not-photo, intensity covers the kept Faint → Intense levels. Parser thresholds stay numeric internally for the view-distance band (ticket 05); only the presentation dropped the `1–5/6–10/11–15/16+` ranges.
- **Both caps at once, no toggle** – two stacked `figure`/`figcaption` canvases (Northern / Southern polar cap), 360×91 at 1px per cell with `image-rendering: pixelated`. The dark lines were styling, not data (grid verified complete 360×181): the 2px tile `gap` showed stage black through the transparent canvas, 2× cells downscaled to panel width blurred transparent seams into the glow, and the equator row painted outside the old 180px canvas. All three are gone by construction.
- **Keyless tiles (no tokens needed)** – Stadia serves HTTP 401 without an API key (verified live) and none exists for this deploy, so the layer switched to CARTO `dark_all` (no key, night-readable) with `© OpenStreetMap © CARTO` attribution; `vite.config.ts` `stadia` route became `carto` (`CacheFirst` 20 entries / 7 days), `vite.config.test.ts` and the component build-check test follow it. No geocoding/routing from the map.
- **Legend + info** – legend is now None (gray `--color-gray` absence swatch) + Faint/Moderate/Strong/Intense with the kept frozen colors, no numbers, no hatch (ticket 06). Card chrome removed – the section lives directly in the Aurora Now panel. `Forecast, not a photo…` sentence removed; the visibility copy is now `Cloud coverage, moon phase and light pollution affect visibility.` behind an `i` `.btn--icon` in a space-between heading row (native `details`, state-driven so jsdom and browsers agree, Escape/outside-close per ChartHelp discipline), plus the `local brightness – not the Kp storm scale` clarifier. Hidden table is now Glow level × North/South cells.
- **Tests** – `OvalGlow.test.tsx` (13: fetch-once, both caps + no toggle, number-free canvas names, heading, Forecast Time, number-free legend + gray None + no hatch, hidden table, CARTO tiles/attribution + no Nominatim/Stadia, info icon disclosure, never-a-photo, offline stale, never-loaded error, CARTO CacheFirst build check); `AuroraNow.test.tsx` integration asserts both glow canvases. Suite green: `tsc --noEmit`, 604 Vitest (66 files), `vite build` (+ PWA `sw.js`). Still not committed – review first.

## Comments (2026-09-05 follow-up, tile-less map + single freshness line)

- **No tokens needed – tile layer deleted.** CARTO returns HTTP 200 but bakes an `API KEY REQUIRED` watermark into the pixels (user-sighted; Stadia 401s outright, Esri/ OSM would be the next third-party dependency to gate or restyle). The glow now paints on a tile-free canvas: black panel background plus its own faint 30-degree graticule (`ovalGraticule`, `--color-gray` token at low alpha, drawn under the cells). Zero tile requests (only the OVATION fetch fires), nothing to key/bill/watermark, fully offline after first fetch. The `carto` runtime route is gone from `vite.config.ts` again (`swpc` + `ovation-jpg` remain); the stray-line class is gone with it – the basemap coastlines/borders/joints were the `many lines`, on top of the earlier tile gap and downscale seams.
- **One freshness line.** `Forecast Time … – 30–90 min lead` and `As of …` repeated the same stamp, so they merged into `Forecast Time {ft} – 30–90 min lead • Updated {age}` (age still from `observationTime`). `FreshnessLine` import dropped.
- **Tests** – tile/attribution assertions replaced by tile-free + fetch-only-OVATION assertions, new `ovalGraticule` unit test (12 + 2 lines per cap), single-line freshness test asserting the stamp appears exactly once. Suite green: `tsc --noEmit`, 603 Vitest (66 files), `vite build` (+ PWA `sw.js`). Still not committed – review first.

## Comments (2026-09-05 follow-up, real gap root cause + keyed Stadia)

- **The gaps were `Math.floor` float error – proven.** Live grid fetched and verified complete (65,160 cells, 360×181, zero missing combos), so paint was dropping columns: `floor((w/360)*360)` refuses 20 columns (lon 5, 16, 27, 28, … – reproduced in node and JS) to true 1px holes that scaling up widens. `projectOvalCell` now rounds + clamps (exact for the integer grid), with a regression test asserting all 360 columns and 91 rows paint per cap plus the old trap values. The gray graticule is gone with the tiles.
- **Stadia is back – with your key.** Fair challenge: the zero-dependency detour was my over-rotation after two gated providers. `ovalTileUrls(hemisphere, apiKey)` builds `tiles/.../alidade_smooth_dark/1/{x}/{y}.png?api_key=` URLs; the component reads `VITE_STADIA_API_KEY` (local `.env`, gitignored – put the key there; Netlify deploy env for production) and omits the tile layer + attribution entirely when unset. Restrict the key to the app domain in the Stadia console; the `stadia` `CacheFirst` route is back (4 tiles, 7 days, key sent rarely). Tiles render flush (no gap) under the fixed canvas.
- **To enable locally:** add `VITE_STADIA_API_KEY=your-key` to `.env` and restart dev. Nothing to commit – `.env` is gitignored.
- **Tests** – keyed-tile assertions (4 flush tiles, key in URL, Stadia/OSM attribution, no Nominatim), no-key omission test, projection regression test, stadia `CacheFirst` build check. Suite green: `tsc --noEmit`, 606 Vitest (66 files), `vite build` (+ PWA `sw.js`). Still not committed – review first.

## Comments (2026-09-05 polish round, pre-commit)

- Heading `Oval glow intensity` is now `h3` (sibling of the JPG section's `h3` under the panel `h2`); captions, canvas names and hidden table read Northern/Southern hemisphere; legend centers via `justify-content: center` (+ `text-align: center`); the info trigger is the real MUI `InfoIcon` (`@mui/icons-material/Info` direct import per the barrel-import rule) at `fontSize="small"` inside the `.btn--icon` summary. Tests assert the `h3` level, hemisphere copy and icon class. Suite green: `tsc --noEmit`, 606 Vitest, `vite build`. Still not committed – review first.

## Comments (2026-09-05 consistency round, pre-commit)

- `ChartHelp` default trigger is now the MUI `InfoIcon` in `.btn--icon` (was the `?` `.btn--secondary` pill), so every SolarWind/Magnetosphere/Forecast chart help matches the glow info icon; naming stays `title` + `sr-only` per the icon-only rule, moon badge custom summary untouched. SolarWind/Magnetosphere tests assert icon triggers now. Tile attribution shrunk to license fine print (0.75rem) with the compliant `OpenStreetMap contributors` credit – the Stadia line is a license requirement while tiles render, and the panel's single `Source: NOAA/SWPC` already covers both JPG and glow data. Suite green: `tsc --noEmit`, 606 Vitest, `vite build`. Still not committed – review first.

## Comments (2026-09-05 heading/icon color round, CSS only)

- Info/help triggers (`.live-panel__help` + `.oval-glow__info` summaries) render in heading color (`--color-accent-strong`) and invert to white on hover like links; other `.btn--icon` controls keep quiet gray. `.live-panel__head h3` moved from `--color-accent` to `--color-accent-strong` (the one heading that wasn't), and the `Pages.scss` heading rule now covers `h4–h6` too. No tests touched; style-contract tests + build green. Still not committed – review first.

## Comments (2026-09-04 oval rewrite: one world map, continuous ramp, GIBS tile)

- Investigation first: the "green patch below the oval" and the full-width green lines at the map bottoms are real feed cells, not paint bugs. The patches are diffuse aurora (lat 45-48 N / 40-48 S, value 1). The lines are OVATION grid-edge rows: lat 0 (324/360 cells) and -1 (319/360) nonzero with all-zero lat 1 and -2; lat -90 (345/360) next to an all-zero -89. Equirectangular smears each across the full width; NOAA's own render hides them by fading low values toward transparent.
- Rewrite (ADR-0007): single 360x181 canvas (pole to pole, integer mapping - the old float-projection seam class is gone), continuous OVAL_RAMP_STOPS ramp painted via ImageData (alpha stops pre-scaled to bytes; band thresholds untouched for table/view-distance/color-blind mode), boundary rows lat  /-1/90/-90 clipped from paint and counts, info popover gains a one-line diffuse-glow explainer (no jargon).
- Basemap: keyed Stadia out, keyless NASA GIBS in - one VIIRS_Black_Marble EPSG:4326 z0 tile (whole 2:1 world in the top half of a 512x512 square; the 2:1 stage restores proportions, pixel-aligned with the grid), rightness(1.7) saturate(0.9) (user-tuned at 60% of the prototype slider against the live tile). The layer 500s intermittently (verified: 200, 200, 500), so the component falls back to a fixed-dim Blue Marble tile and retries Black Marble in the background (8 tries, 1.5 s apart), swapping it in on first success. gibs CacheFirst route (6 entries, 1 yr) replaces stadia; VITE_STADIA_API_KEY unused everywhere; NASA imagery needs no attribution line.
- Freshness line back to the ADR-0006 one-line form (... 30-90 min lead - Updated {age}) - it had been commented out, leaving two committed OvalGlow tests red.
- Tests: OvalGlow suite rewritten (world-map canvas, legend gradient from the shared stops, boundary-row count exclusions, GIBS tile + fallback, ramp alpha regression, gibs CacheFirst build check); AuroraNow integration now 1 glow canvas; vite.config.test gibs route. Full suite: 	sc --noEmit green, 606/609 Vitest (3 pre-existing failures in PinnedWebcams/Home.dashboard verified on the clean baseline via git stash - not this change), ite build + PWA sw.js green. Still not committed - review first.

- 2026-09-04 (later) - basemap swapped GIBS tiles -> bundled Natural Earth 1:110m land GeoJSON (public/ne_110m_land.geojson, public domain) painted on an aria-hidden canvas through the same ovalCellPoint projection. Cause: GIBS's Black Marble generation went unstable server-side (same URL served a full world, then a right-cut world, then a level-0 pair split near lon +60-90E against its own capabilities doc, plus constant 500s); user-visible as a cropped/stretched world no CSS could pin. Ramp recolored after user feedback: green now carries the whole ordinary range (1-15, matching NOAA's all-green render), yellow from 16 (storm onset), red/magenta for extremes. Stage aspect pinned 360/181; gibs SW route removed; geojson added to PWA precache. ADR-0007 revised.

- 2026-09-04 (later still) - land-paint artifacts fixed from user's annotated screenshot: (1) full-width line at lat ~-17 + missing Antarctic bottom + partial -83 fill all traced to antimeridian-wrapping rings self-intersecting the naive %360 projection - fixed by ring longitude unwrapping (projectRing), painting at -360/0/+360 offsets, and closing >180-degree-span rings via the nearer polar edge (Antarctica now fills to the bottom row); (2) polar-cap diffuse rows painted cell-raw read as glitch bands - fixed with a 3x3 box blur on the glow frame (lurGlowFrame), NOAA-render style; (3) land fill deepened-contrast to #666666 per user pick (3.66:1 on black, was 1.36:1), with a contrast regression test. Row-dump verified: no non-geographic full-width rows remain.
