# 03: Interval-text tables + panel shell with text tab

**What to build:** the interpreter's content seam and its Home on screen — four pure interval-text tables (live Kp, speed, Bz-as-gate, hemispheric power: 3 levels each, both P1/P2 variants drafted, source keys attached) wired into a new Home panel whose text tab renders one honest sentence per live row with caveat links and deep links into the existing expert cards. (Merges proposed 09+10.)

**Background:** content decisions in ticket 01 Part C; sentence sources in the honesty inventory (`docs/research/plain-language-honesty-2026-09-12.md`); full behavior in `spec.md`. The human picks P1/P2 per row at ticket 08 — this ticket drafts BOTH variants per row. Prototype ticket 04 may already have draft sentences worth lifting (note it if so, trim to the decision-rich wording).

**Blocked by:** None (can start immediately — foundations in ticket 01 are done).

**Status:** done

- [x] Tables are pure data over the existing product hooks (no new fetching): value → level, sentence, source. Kp lines quote the SWPC Tips bands with both caveats; Bz speaks gate language only ("favors/can drive", sustained-hours) — the app's old Bz→Kp lookup must not feed the tables; speed always paired with the direction caveat; HP carries the model-uncertainty budget
- [x] No color promises anywhere (N4): no sentence assigns green/red/purple to a place or time, oval red is never read as bad, and far-south viewers are told to expect red; no Kp↔glow conflation (N9): planetary Kp and local oval cells never interchangeable in any sentence
- [x] Unit pins per interval including boundaries, estimated/predicted Kp tags, and missing-data states ("no data", never zero); 1-min L1 readings labeled "latest reading", never "conditions"
- [x] Expert Bz card fixed at the source (absorbs ticket 09): its helper rows ("active (Kp3-4)" etc.) rewritten as gate language with sustained-hours framing — no band promises a Kp outcome — so expert detail and plain layer never contradict; its tests updated, no Kp-outcome assertions surviving
- [x] Panel renders on Home with working tabs (text first), As-of/stale notices and display-timezone times like every other surface; expert numbers, charts, tables and explainers untouched
- [x] Typecheck + Vitest green

Done 2026-09-12 (TDD, confirmed seams): `src/products/interval-texts.ts` (kp/speed/bz/hemi-power, 3 levels each, stable sentence keys, null → "no data") + `Interpreter` panel above Aurora Now; expert Bz help rewritten as gate language (absorbs old ticket 09). HP legend verified live 2026-09-12 (inventory §8). Boundaries as confirmed: Kp calm <3 / active [3,6) / storm ≥6; speed calm <400 / active [400,700) / storm ≥700; Bz calm >0 / active (−10,0] / storm ≤−10; HP calm <20 / active [20,50] / storm >50 (North).

**Amended same day (human decision in review):** the panel shape changed from four cards + P1/P2 variants to ONE merged paragraph (one concise sentence per row, no P1/P2, no value chips) — the P1/P2 drafting experiment is closed and ticket 08 reviews the single wording per row. Consequences: the per-row freshness lines collapsed into one As-of line for the oldest feed; the Kp observed/estimated tag note and the G-scale chip died with the value chips (ticket 08 may reinstate the G label if wording wants it); a Home-level test pins one network fetch per feed across all mounted panels (query-key dedupe).

**Amended again same day (human decisions q1–q4 in review):** hemispheric power is DROPPED from the summary (OVATION's synthesis of the same L1 inputs — stays expert-only in Magnetosphere); the caveat and sources lines are removed (the SWPC caveats live in the tables' source keys and tests, not in shipped copy); the deep links are removed with the separate panel — the summary sits directly above the expert cards in Aurora Now, so "dig one tap deeper" needs no link; the L1 claims run on 5-minute averages, never single 1-min readings — `averagedValueAt` in live-panels, and the Solar Wind panel's displayed current values switched to the same 5-minute averages with its note extended ("The displayed current values are 5-minute averages."); the time-ahead selector stays (arriving now → ~15 → ~30 → ~transit min, offered offsets capped by the transit horizon). The separate "In plain language" article is REMOVED — the summary now lives at the top of the Aurora Now panel body, always fully open (the Read more toggle and its persisted preference were removed by the human's next review pass, taking `products/summary-storage.ts` with them). The summary's As-of covers only its L1 readings so the panel's Kp freshness line is never duplicated. Follow-up (human screenshot): the raw "Couldn't load – connect to refresh" never renders mid-paragraph — a feed still pending (or retrying) leaves its sentence out until it lands, and one that failed for good reads the honest "No data right now."; the L1 feeds are independent, so a transient rtsw-wind hiccup while Kp and the mag field land is exactly what produced the earlier mixed paragraph. Follow-up 2 (human): the summary's As-of names the FEEDS' real freshest-reading time (oldest of the two L1 feeds' latest rows) and never moves with the time-ahead selector — the selector picks which measured reading the sentences read; the update time is one fact, one line, the same for every interval.

**Amended 2026-09-13 (human wording review, ticket 08 brought forward for the tables):** the Bz row's gate jargon ("magnetic gate", "cracked open", "open wide", "northward") told a novice nothing and is gone — the field's direction is plain "the right way / the wrong way for aurora", with the sustained-hours framing kept ("if it holds for hours, it can drive a storm"). Bigger change: the speed and Bz rows are MERGED into one L1 sentence (`l1IntervalText(speed, bz)` in `src/products/interval-texts.ts` replaces `speedIntervalText` + `bzIntervalText`) — since the field decides what the speed means, the human wanted the field mentioned once, not twice. The merged sentence names the stream's speed plainly (slow / fast / very fast) and the field's direction in the same line; the level is the sentence's honest severity — a northward (wrong-way) field caps the level at active however fast the stream (N2 made structural), only a southward field or a very fast stream leaning southward reaches storm-like (N1 kept: no Kp outcome from a band). Either L1 feed missing reads the honest "No data right now." row — never a half sentence. The summary now renders TWO sentences (Kp + the merged L1 line); the graphical-meters ticket 05 must follow this shape (2 meters, one shared level source). The gate framing survives in the expert Bz card (SolarWind panel), which stays as-is. Kp wordings untouched (human approved with own tweaks); the "enjoyable from the right place" phrasing stays, and the human's question about a towns UI element is answered in ticket 08 (no ticket exists; N11 keeps city strings out until verified live — new effort required).

**Amended again 2026-09-13 (human: density bonus + keyword highlighting):** the human simplified the clause tails (dropped "(poor)/(good)/(excellent)" parentheticals and the strongly-right clause's sustained-hours tail — now "…pointed the right way for aurora and pushing hard."), then asked for particle density as a bonus adjective and keyword highlighting. Shipped: `l1IntervalText(speed, bz, density)` — density rides the SAME rtsw-wind feed (no new fetch), averaged at the same 5-min instant as speed, banded honestly light (<10 p/cm³) / dense (≥10) per inventory S2 (the app's finer rows are an unbacked convention; two steps avoid inventing precision) — the sentence reads "…running fast and dense, and the magnetic field…" (N2 intact: density never decides the level, it only describes the stream). Keyword highlighting: each clause carries a `SentenceMark` (exact substring + good/bad polarity), rendered by `MarkedSentence` in AuroraSummary as spans colored `--color-accent-strong` (good) / `--color-orange` (bad) — color reinforces the word, never replaces it (WCAG; the word stays for screen readers and color-blind users). Kp rows carry no marks. Tests pin every mark as an exact non-overlapping sentence substring (a missing substring drops the highlight silently — pinned so it fails loudly).
