# 08: Wording review pick + verification pass

**What to build:** the human picks P1 or P2 per row (loser deleted, not flagged), then the whole panel is proven shippable — full unit + Playwright + axe + narrow-layout pass with typecheck and build green.

**Background:** prototype ticket 04 frames the comparison; ticket 03 ships both variants in the tables. This ticket closes the voice question and the effort's build.

**Blocked by:** 05 (meters), 06 (sky line).

**Status:** done

## Review record

- [x] Human review recorded per row in this file (P1 / P2 / merged per row); losing variants removed from the tables entirely. 2026-09-13: the table half of this already happened in ticket 03's amendment (gate jargon dropped, speed+Bz merged into one `l1IntervalText` sentence, Kp wordings human-tweaked) — this ticket reviews whatever remains plus the full panel
- [x] Human question answered 2026-09-13: a "towns where it may be visible" UI element does NOT exist and is deliberately out of scope (N11 unverified city strings; ticket 06 says "no city strings", spec's preferred alternative was to skip cities). If wanted, it is a NEW effort whose first ticket verifies city strings live against an official source — record here whether to open that effort or waive it
- [x] Hallway check with a novice (Q8, soft-maybe): a layman answers "can I see aurora tonight from my place, when/where do I look?" unaided — performed with a willing newcomer if one is at hand, otherwise explicitly waived here with the reason recorded
- [x] [RE-VERIFY] items from the honesty inventory (ticket 01 Part B: city strings, HP legend wording) re-checked live before ship — unresolved items stay out of shipped copy
- [x] Playwright Home journey on live-shape fixtures: panel renders, tab keyboard path, daylight-gate states, axe audit clean, narrow-layout pass
- [x] Full suite green: Vitest + Playwright + typecheck + build

Done 2026-09-14 (TDD, verification pass; every shipped sentence is the single post-experiment wording — no P1/P2 twins remain):

**Wording pick recorded (human, 2026-09-13 review; re-confirmed 2026-09-14).** The P1/P2 experiment closed before this ticket (ticket 04 → wontfix; ticket 03's 2026-09-13 amendment), so there was no loser left to delete — the losing variants were removed in that amendment. The shipped voice per row: Kp via `kpIntervalText` (calm "Aurora intensity is currently low, so any glow may stay far north and faint." / active "Aurora is possible now, brighter and moving – enjoyable from the right place." / storm "Strong aurora is possible, reaching farther south than usual. (Far-south viewers mostly get red, not green.)" — human-approved with own tweaks); the L1 sentence via `l1IntervalText(speed, bz, density)` (speed + density + field direction merged because the field decides what the speed means; density rides as a bonus adjective; keyword marks only reinforce the words); hemispheric power dropped from the interpreter (expert-only); the Moon caveat (`moonWashoutText`) and the View distance reach (`viewDistanceText`) carry no variants. The View distance label names the light when the sky is too bright: "Daytime" / "Civil twilight" / `Aurora {confidence}`.

**City-strings question resolved 2026-09-14: a new effort is opened**, not waived — `.scratch/aurora-city-strings/` with its first ticket verifying the per-G city strings live on `spaceweather.gov/noaa-scales-explanation` before any copy ships. The strings were read live during this pass (inventory §9) but none ship in the panel (N11); the first ticket re-verifies at its own time and decides whether a "towns" element is wanted.

**Hallway check waived 2026-09-14:** no willing newcomer was at hand during the build session, and Q8 was explicitly soft-maybe. The owner can still run the 5-minute first-use check post-review; the panel's honesty bounds (may-not-will, no promises, no cities) and the Aurora guide are the shipped mitigations.

**[RE-VERIFY] pass 2026-09-14 (live fetches, recorded in the honesty inventory's §9).** Kp wording: the SWPC Tips on Viewing the Aurora page confirms every shipped Kp sentence's band (0–2 far north quite dim / 3–5 brighter with motion and formations, pleasing from the right place / 6–9 farther south) and the page's own approximate-averages and geomagnetic-not-geographic caveats verbatim; shipped copy keeps may-not-will and carries no city strings. City strings: the NOAA Scales page was read live — per-G strings exist (G1 northern Michigan/Maine, G2 New York/Idaho, G3 Illinois/Oregon, G4 Alabama/northern California, G5 Florida/southern Texas) — and none ship; handed to the new effort. HP legend wording: the interpreter carries no HP row (dropped 2026-09-12), so no unresolved HP sentence can ship; §8's verification stands for the expert rows. The Aurora 30-minute page re-confirmed the HPI definition, the 30–90 min lead, the 1000 km sighting distance, the daylight no-go and the Kp-fallback "no forecast lead time" — the fallback stays a recorded residual (no flag on the product).

**Playwright Home journey (TDD):** new `e2e/home-interpreter.spec.ts` (7 tests) on live-shape fixtures with pinned clocks — panel renders (Kp + merged L1 + reach + one guide link); the time-ahead selector's keyboard path steps Now → 15 → 30 with arrow keys and drives the L1 sentence ("leans weakly toward aurora" ↔ "pushing hard"); the daylight gate reads "Daytime" / "Civil twilight" / "Aurora likely" at pinned instants (10:00Z / 18:20Z / 23:30Z at Luleå, 2026-08-26); an `.aurora-now`-scoped axe audit is clean; and the panel fits 390 px with no horizontal overflow. Tabs and the sky line no longer exist (recorded human simplifications in tickets 05/06), so "tab keyboard path" lands on the selector and "daylight-gate states" on the surviving View distance label.

**Full suite green 2026-09-14:** 891 Vitest (85 files) + 78 Playwright + `tsc --noEmit` + `npm run build`. Two pre-existing reds were found by the pass and fixed: `Guide.test.tsx` expected accessible names "Night"/"Oval" that the guide never rendered (prose labels are lowercase "night" and "aurora oval"; the test was aligned, red → green); and `src/styles/_typography.scss` had been stepped at `lg` by commit fb4df04 against ADR-0009 and `e2e/typography.spec.ts` — restored to `respond-to(md)` (the ticket 09 record claimed this restore, but it was not in the tree). Both fixes are declared here for review.
