# Map: Plain-language aurora for laymen and travelers

## Destination

A spec to hand off (nothing built in this effort): a plain-language interpreter panel on Home with text + graphical tabs, plus a "What are auroras" guide page in the About submenu — so a layman or traveler with no scientific background can answer "can I see aurora tonight from my place, when and where do I look?" without the app removing any existing expert info.

## Notes

- Domain: single context — root `CONTEXT.md` plus `docs/adr/`. Every session reads the glossary terms it touches (View distance, Night, Local conditions, Geocoded place, Oval, Alert threshold, Kp index, Planetary K-index (live), Bz (GSM), Hemispheric power, Dst index, Weather (Local conditions), About submenu) and uses them verbatim; new terms (interpreter, interval text, quick check, aurora guide) graduate via domain-modeling, never as synonyms.
- ADRs in play: 0001 client-side-only (constraint DEFERRED per Q3 — stays default, overturned only if a ticket forces it), 0003 live polling, 0005 local conditions + manual refresh, 0006 offline PWA + personal oval, 0007 one-world oval + continuous ramp, 0008 display timezone defaults to local.
- Skills every session consults: grilling + domain-modeling. Standards: `docs/agents/coding-standards.md` (TypeScript strict, SCSS + BEM, Vitest + Playwright, WCAG 2.1 AA).
- Standing preferences from charting (Q1–Q14): keep all expert info, layer ease on top, no reorganization (Q9); audience is laymen + travelers, not experts-only (Q5); trip multi-night ranking is out — saved-place picking is enough (Q10); interpreter ships as TWO prototypes to compare: P1 names the values while explaining them, P2 gives clear interpretation with no scientific noise/jargon (Q13); interpreter panel has text + graphical tabs (Q12); honest-by-construction — never promise, every line carries likelihood + mapping source + cloud/moon/light-pollution caveats, and local sky data is folded in where available (Q14); validation by 5-minute first-use test is soft-maybe (Q8).

## Decisions so far

- [01: Foundations (merged 01+02+03+07)](issues/01-foundations.md): pitfalls audit + honesty inventory with 12-item NO-GO list + interpreter content decisions (4 rows × 3 levels, both tabs, gated sky line) + guide scope (5-section outline, latitude-aware where-to-look, "Guide" at `/about/guide`, spin-off popups) — done; facts in `docs/research/plain-language-pitfalls-2026-09-12.md` and `docs/research/plain-language-honesty-2026-09-12.md`, build spec in `spec.md`. Absorbed files removed.
- Build tickets 02–09 published from the specs and numbered clean (02 moon wiring; 03 tables+shell+expert-Bz fix; 04 merged prototype; 05 meters; 06 sky line; 07 inline glossary popups; 08 review+verification, last; 09 guide page build). Specs: `spec.md` (panel) + `guide-spec.md` (guide).
- [02: Moon position wiring (done 2026-09-12)](issues/02-moon-position-wiring.md): `src/data/moon.ts` — altitude gate, rise/set per day over suncalc v2, pinned to time.now literals in `docs/research/plain-language-moon-2026-09-12.md`. The sky line's moon half un-cages with ticket 06.
- [03: Interval-text tables + panel shell (done 2026-09-12, amended thrice same day)](issues/03-tables-and-panel-shell.md): `src/products/interval-texts.ts` (Kp/speed/Bz rows × 3 levels, stable keys, null-safe; HP dropped) + the plain-language summary inside the Aurora Now panel (always open — the Read more toggle was removed in review; time-ahead selector over the L1 readings; 5-minute averages via `averagedValueAt` — also adopted by the Solar Wind headline values); expert Bz help rewritten as gate language (absorbs old ticket 09). HP legend verified live — see the inventory's §8 verification log. The P1/P2 wording experiment closed (ticket 04 → wontfix; ticket 08 reviews the single wording). New glossary terms graduated into `CONTEXT.md`: Interpreter, Interval text, Plain levels.
- [05: Graphical meters tab](issues/05-graphical-meters-tab.md): wontfix — the human's 2026-09-13 decluttering removed the panel's tabs and merged the L1 rows into one sentence, so the graphical rendering has no shape to render. Standing preference Q12 (text + graphical tabs) is overtaken by that later decision; recorded in `CONTEXT.md`'s Interpreter definition.
- [06: Sky line + daylight gate (done 2026-09-13)](issues/06-sky-line-and-daylight-gate.md): `src/products/sky-line.ts` (pure composer, 20 unit pins) + `SkyLine.tsx`/`.scss` wired below the interpreter paragraph. Night band gates first (`dark-now` / `daylight` / `polar-day`; `lookNow` only when dark), then cloud (under/over half, honest percent, "unavailable" on a missing feed), the Moon instant gate from ticket 02 (modifier only when dark and up — N7), and the View distance band with its confidence and 30-90 minute lead (the new `viewDistanceBand` accessor). Visibility caveats ride only the look-now line; reach always carries the averages + geomagnetic-not-geographic caveats, no city strings. Shared `useWeather` hook dedupes the Open-Meteo fetch with `CurrentWeatherLine`. Residuals recorded in the ticket: the OVATION Kp-fallback lead-time gap (no flag on the product) and the Moon's instant-vs-window scope. New glossary terms: Sky line, Daylight gate. **Human simplification (2026-09-13, later same day):** View distance line moved above the interpreter paragraph and the Sky line moved into the Oval glow panel; the line's cloud fetched-at stamp, repeated 30-90 min lead and approximate-averages/geomagnetic caveat were dropped (may-not-will kept). **Bright-sky label:** the View distance line now reads "Daytime" / "Civil twilight" instead of an aurora band while the sun is too bright (`sunState` in `data/sun.ts`), the band returning only when darker. **Final simplification (2026-09-13):** the Sky line was dissolved entirely — `SkyLine.tsx` + `products/sky-line.ts` deleted — with the dark window moved to the Current weather line as `Darkest (band): start to end.` (`darkestWindow` in `data/sun.ts`, `Polar day.`/`all day` for the polar states), the cloud clause dropped as redundant, and the Moon caveat (up + lit, `moonWashoutText`) and reach (`viewDistanceText`) appended to the interpreter paragraph. New glossary term: Darkest; Sky line / Daylight gate removed.

- [07: Inline glossary popups (done 2026-09-13)](issues/07-inline-glossary-popups.md): glossary entries extracted from `explainers.tsx` to the shared `src/components/explainers/glossary.ts` (`GLOSSARY_ENTRIES` + `getGlossaryEntry`); `GlossaryTerm` renders a real focusable `<button>` that opens the entry through `HelpPopover` with a quiet "Read the full glossary" anchor link, and unknown ids fall back to plain text. `HelpPopover` was refactored (user-approved) from `<details>/<summary>` to a `<span>` + `<button>` + a body portalled to `#main-content` and placed with fixed coordinates (`placePopover`), because `<details>` is flow content and invalid inline in the ~20 prose `<p>`s and the LiveBanner `<h2>`; shared body chrome moved to a single `.help-popover` base, the `summary` prop was renamed `trigger`, and opening now moves focus into the body with a Tab/Shift+Tab trap and focus return to the trigger on every dismissal. Four glossary entries were authored (`interplanetary-magnetic-field`, `bz-gsm`, `hemispheric-power`, `dst-index`) because the Home live banner linked to non-existent anchors; the Explainers page grew 18 → 22 entries. Also fixed pre-existing copy drift (`Darkest (night):`; the Color-blind pill label back to CONTEXT.md's "Color-blind mode"). No new CONTEXT terms.

## Not yet specified

- (The panel-build fog has cleared: the interval sentences landed in ticket 03, the sky line in ticket 06, and the graphical meters were dropped with ticket 05. What remains is the standing build order below.)
- Remaining build tickets: 08 wording review + verification (last, gated on 06), 09 the guide page build.

## Out of scope

- Multi-night trip ranking / travel-itinerary planning (ruled out Q10: place-picking already covers it).
- Reorganizing or removing existing expert detail (ruled in Q5/Q9: disclosure layers on top only).
- Backend, accounts, push, proxy (not decided — deferred per Q3; returns only if a ticket proves client-only can't do it, as a fresh effort redrawing the destination).
- Localization or editing NOAA content (carries over from the modern-stack migration scope).
