# 01: Type tokens, breakpoints foundation, and migration of shared surfaces + home page

**What to build:** The complete type-token system lands and the chaser immediately sees it working across the app chrome, shared surfaces (buttons, tables, live panels, sources, pages) and the home page: every heading is sized by global element rules from semantic Type tokens, body text carries the body token, and the type scale steps at the canonical Breakpoints. The responsive mixin exists and is the only sanctioned way to write a media query in touched files.

**Blocked by:** None (can start immediately).

**Status:** done

- [x] The typography partial defines all font-size and paired line-height Type tokens on the root with the ADR-0009 values, including the md-breakpoint steps for body, lead, h1 and h2
- [x] The breakpoints partial defines the canonical Breakpoint map (sm 480, md 810, lg 1100) and the `respond-to(sm|md|lg)` mixin, mobile-first min-width only; both partials reach the global entry stylesheet via `@use`
- [x] Global h1–h4 element rules size headings from the tokens; the body element carries the body token
- [x] All font-size and line-height declarations in the global entry stylesheet and the shared surface stylesheets (buttons, sr-only, skip-link, image modal, charts overrides, pages, tables, live panels, sources) use Type tokens; `em` sizes are collapsed to rem tokens
- [x] All font-size and line-height declarations in every home-page component stylesheet use Type tokens; the three large data readouts remain as the documented literal exceptions, with the stray `!important` removed
- [x] Per-component heading-size rules in migrated files are deleted in favor of the global rules
- [x] Media queries in migrated files go through `respond-to`; the 480/481, 810/811 and near-1000 legacy pairs in migrated files fold into canonical Breakpoints
- [x] Grep over migrated files shows zero raw rem font-size values and zero raw-pixel media queries outside the frozen readouts
- [x] Playwright computed-style checks pass at a width below and above md (h1, h2 and body provably step); the existing a11y specs pass
- [x] Visual pass over home, navigation chrome and shared surfaces at phone and desktop widths shows no layout breakage

## Comments

- Implemented 2026-09-10, TDD red→green on the one new seam. RED: `e2e/typography.spec.ts` asserted the ADR-0009 computed sizes at 700px/900px and failed exactly as predicted (body 16px ≠ 0.95rem below md; h1 32px ≠ 40px and h2 24px ≠ 28px above md). GREEN: `src/styles/_breakpoints.scss` (SCSS `$breakpoints` map + `respond-to(sm|md|lg)` mixin emitting min-width only) and `src/styles/_typography.scss` (the eight font-size tokens with paired unitless line-height tokens, global `h1`–`h4` element rules, `body` on the body token, and the md step redefining only body/lead/h1/h2 on `:root`); both reach `index.scss` via the codebase's first `@use` wiring (`@use "styles/typography"` first, `@use "styles/breakpoints" as *`). The About page carries the computed-style assertions because home's panel headings are em-chained (CollapsiblePanel, ticket 02); the strengthened suite also asserts h3 (1.25rem, both widths) and a caption-role element (`SourceAttribution`, 12px, both widths) per the spec's h1–h4 + captions testing decision — no h4 exists in the markup, so h4 is unassertable.
- Migration slices after the foundation: `index.scss` (`.btn` small, `input` body, the ~1000px button-collapse block restructured mobile-first — collapsed is the default, `respond-to(lg)` restores labels and pill padding, the legacy ~1000px width folding into lg per ADR-0009); shared surfaces Pages/Tables/sources (`article` and banner pills line-heights onto the body token, `pageIntroduction` and table captions onto lead, `utc-note`/attribution/sub-header cells onto small/caption); live-panels (h3 rule deleted, popover/note/warning/unit small, `__fresh` caption, 810px gap pair folded into `respond-to(md)`); home components Alerts/AuroraNow/CurrentWeatherLine/OvalGlow (six smalls)/ViewDistanceLine/Forecast/kp-panel/LiveBanner/PinnedWebcams (h3 title rule deleted, attribution caption)/Home (810/811 pairs folded into `respond-to(md)` — grid and padding inverted mobile-first). `webcams.scss` touched only to strip the pin glyph's stray `!important` (rest of the file migrates in ticket 02).
- Judgement calls for review: (1) **Line-height scope** — the ticket checkbox reads "all font-size and line-height declarations … use Type tokens", but ADR-0009's enforceable consequence is font-sizes; role-bearing text line-heights went onto the paired tokens (article, LiveBanner pills, CurrentWeatherLine, ViewDistanceLine, `.btn`), while two tuned data-chrome line-heights stay literal — the forecast mini-table day cell (1.15) and the Kp bar's segment height (1rem, which IS the bar's height) — now documented in ADR-0009's frozen list and the standards doc; tokenizing them would change data geometry for no gain. (2) **Fourth frozen literal** — the moon emoji badge (2.7rem, `live-panels.scss`) is an icon glyph like the webcam pin, not a type role; ADR-0009's frozen list was corrected en route (its "aurora temperature"/"webcam count" names had drifted — the actual literals are the Aurora Now Kp readout, the live-panel value family, and the two glyphs) and now names all four plus the two line-heights; ticket 03's doc-drift check should re-verify. (3) **Orientation query** — `OvalGlow.scss`'s `@media (orientation: portrait)` stays raw: `respond-to` emits width Breakpoints only; documented in the ADR consequences and a code comment. (4) The button-collapse fold (999px → lg 1100px) is a real UX delta in the 1000–1099px band (labels now collapse there) — sanctioned by the ticket's fold rule, called out so it isn't a surprise. (5) The collapsed/expanded button-label states are two explicit 9-property blocks (collapsed default + lg inverse) rather than a dedupe mixin — the pairs differ in `margin`/`white-space` and the inverse pairing is the point; a parameterized mixin would obscure it (review flagged the duplication, kept deliberately). (6) `Pages.scss` `.pageIntroduction` (lead token) has no TSX consumer found — migrated rather than deleted; deletion is a maintainer call. (7) Transitional em-chain delta: CollapsiblePanel's `font: inherit` button and chevron now inherit the body token (0.95rem mobile), so panel h2s render 1.425rem until ticket 02 migrates that file onto the tokens — at md the em chain already reproduces 1.5rem.
- Verification: typecheck clean; Vitest 788/788 (80 files, `ui-palette-final` source-contract included); Playwright fresh-build runs — typography (2), smoke (19), mobile-layout (9), plus home/about/sources/explainers/conditions/webcams and the six product-page a11y specs: the only failures are the six documented environmental ones (5 conditions + home color-blind toggle), re-verified reproducing identically on clean HEAD via stash. Grep over migrated files: only the four documented frozen font-size literals remain, zero raw-pixel media queries. Visual pass via full-page screenshots of home/conditions/webcams/forecasts/about at 390/700/1280px: zero horizontal overflow everywhere, headings step, no layout breakage. Not committed — awaiting maintainer review.
