# 02: Migrate navigation, shared components, conditions, webcams and forecasts onto type tokens

**What to build:** The rest of the app completes the migration: the navigation, shared components (collapsible panels, place finder, popovers, glossary, weather icon, moon chart, modals) and the conditions, webcams and forecast-product pages all render text from semantic Type tokens at the canonical Breakpoints. After this ticket, every text in the app is token-sized — a chaser sees one consistent type system on every page.

**Blocked by:** 01 (tokens foundation, shared surfaces and home page migrated).

**Status:** done

- [x] All font-size and line-height declarations in navigation, shared-component, conditions, webcams and forecast-product stylesheets use Type tokens; `em` sizes collapse to rem tokens
- [x] Per-component heading-size rules in migrated files are deleted in favor of the global h1–h4 rules
- [x] Media queries in migrated files go through `respond-to`; the 419px and 900px legacy widths fold into canonical Breakpoints
- [x] The frozen data-readout exception remains the only literal font-size value in the codebase (grep over all component stylesheets shows no other raw rem/em font-size and no raw-pixel media queries)
- [x] Playwright computed-style checks and the existing a11y specs pass
- [x] Visual pass over conditions, webcams, forecasts, about/sources/explainers and navigation at phone and desktop widths shows no layout breakage

## Comments

- Implemented 2026-09-11, TDD red–green on the existing typography seam: RED extended `e2e/typography.spec.ts` with computed-style checks for the ticket-02 surfaces (home panel h2 22.8→24/28px, webcams tab 16.8→15.2/16px, 27-day explainers 15.2→16px above md, PlaceFinder modal h2 18.4→24/28px, Time modal note 14.4→13.6px, plus a 390px below-sm check 18.24→20px), all failing exactly as predicted; GREEN migrated the stylesheets. Token mappings: 0.7/0.75→caption, 0.8/0.85/0.9→small, 0.95/1→body, 1.05/1→body (webcams tabs), 1.1/1.15→lead; `em` chains collapsed (CollapsiblePanel h2 1.5em → the global h2 rules; chevron 2em → h1 token, glyph-box 3rem line-height documented).
- **Nav.scss mobile-first inversion**: the `max-width: 1100px` hamburger block is now the default (panel, backdrop, right-aligned items) and `@include respond-to(lg)` restores the wide bar — the last raw-pixel width query in the app. Faithful-copy pitfalls found and fixed during review: the panel's submenu links keep the shared 44px touch target, padding and hover/focus chrome (the old file's base rules had applied these at every width; moving base to lg-only would have dropped them below lg); the narrow button margin override needs the `.btn--secondary` class to out-specify the base rule (the old `!important` was load-bearing — specificity, not importance); the two `background: unset !important` lines became dead once the base moved into the lg block and are deleted. The two raw rgba colors in the moved block tokenize (`--color-black` color-mix). **Maintainer-reported regression (2026-09-11), fixed**: the first lg restoration was incomplete — the narrow defaults' `padding: 0 2rem` on `li > a`/`li > details > summary` and the `flex: 1` on `.header__nav` were never reset, so the wide bar rendered with panel-sized paddings (spread-out items, "Local conditions" wrapping to two rows in the ~1100–1300px band). The lg block now restores the link/summary padding to `var(--nav-horizontal-gap)`, the link `justify-content: center`, and `flex: initial` on the nav; verified single-row at 1120/1150/1250/1440px and the panel mode untouched at 900px. **Second leak found the same way (maintainer screenshot: uneven submenu rows)**: the narrow `.header__menu li` was a plain descendant selector, so the panel's `width: fit-content`/`align-self: flex-end` staircase also hit the wide bar's submenu items (rows 126–194px instead of one uniform 200px column, hover background only as wide as the text), and the narrow `li:not(:last-of-type) { border: none }` (specificity 0-3-2) out-ranked the lg `border-bottom` (0-2-2), dropping the wide bar's separators. Fixed by scoping the narrow rule to `.header__menu > li` (top-level items only, the panel staircase moved explicitly into the narrow `details[open]` block) and scoping the whole lg `details[open]` block under `.header` so each lg rule out-specifies its narrow counterpart on specificity rather than order: the lg submenu li is back to the old base's block flow — uniform full-width rows (`width: auto`, `align-self: normal`, `display: block`), `a` filling the row, borders restored. Verified by computed-style dump (all rows 200px, 1px borders) plus screenshots at 1250px wide bar and 900px panel, and mobile-layout + smoke + about-a11y + typography 34/34. The 419px fold in CollapsiblePanel inverts to a below-sm default (h3 token) with `respond-to(sm)` restoring the h2 token — the narrow step is a token now, not a raw 1.2em.
- **The 900px fold resolved by deletion**: the `max-width: 900px` block in `27-day-outlook.scss` was dead code (`grid-template-columns` on a `display: flex` container) — nothing to fold, deleted.
- LuminosityTimeline and the three webcams 480px pairs invert the same way: narrow is the default, `respond-to(sm)` restores the row timeline, vertical band names, auto-fill card grid and span-2 wide cards. Webcams tabs retune onto tokens (1.05rem → body at sm+, 0.9 → small below) — a ≤0.15rem delta, token-forced.
- Judgement calls for review: (1) **PlaceFinder modal h2's 1.15rem rule is deleted** per the heading-rule deletion rule, so the modal heading renders at the global h2 (24px mobile) instead of 18.4px — a visible size-up, consistent with the one-system rule and the Time modal's h3. (2) **WeatherCurrent's 2.3rem/1.6rem/1.1em join the frozen list** (conditions temperature readout: 2.3rem drives the 1.4em weather icon; 1.6rem is the temp; the 1.1em detail icons stay relative so they scale with their labels) — ADR-0009's frozen list and the standards doc extended to name them, following ticket 01's moon-badge precedent. (3) The tuned line-height literal list grew by documentation (webcams prose 1.5rem ×3, weather hour-card box 1.25rem, CollapsiblePanel chevron box 3rem, TimeModal icon-button 0, live-panel values 1.1, Aurora Now readout 2.5rem) — all commented in place and named in ADR-0009. (4) Dead commented legacy blocks (WeatherCurrent legend, WeatherHourly cloud-split) deleted so the frozen-value grep stays honest. (5) The e2e file gained an `expectFontSize` helper collapsing the repeated poll shape.
- Verification: typecheck clean; Vitest 788/788; Playwright — typography 5/5 (3 new below/above-md + below-sm fold checks green after migration), mobile-layout 8/8, smoke 20/20, webcams + offline 11/11, about/sources/explainers/alerts + six product-page a11y 19/19; home/conditions a11y: only the six documented environmental failures, re-verified reproducing identically on clean HEAD via stash; after the nav-regression fix, mobile-layout + typography + smoke + about-a11y re-ran 34/34. Visual pass: full-page screenshots of home/conditions/webcams/3days/27days/about/sources/explainers at 390/700/1280px plus the nav panel at 390/700 and the wide bar at 1280, and after the fix the wide bar re-verified single-row at 1120/1150/1250/1440px — zero horizontal overflow everywhere, headings step, panel and wide-bar layouts both correct. Grep contract: only the documented frozen literals remain; zero raw-pixel media queries outside the mixin and the non-width queries. Not committed - awaiting maintainer review.
