# 03: Contract sweep and verification of the type-token system

**What to build:** The migration contract closes: no raw font-size values or raw-pixel media queries survive anywhere, the coding standards cite the new rules so future code (human or agent) reaches for Type tokens and `respond-to` instead of inventing values, and the whole suite plus a per-page visual pass proves the system is green end to end.

**Blocked by:** 01, 02.

**Status:** done

- [x] Grep over all stylesheets confirms zero raw rem/em font-size values and zero raw-pixel media queries, with only the three documented data-readout literals remaining
- [x] The coding-standards document records the type-token rule and the `respond-to`-only media-query rule, citing ADR-0009, with the frozen readout exception noted
- [x] The glossary Type token and Breakpoint entries match the shipped system (no doc/code drift, unlike the status-red drift found under ADR-0002)
- [x] Full Vitest and Playwright suites pass, including computed-style checks at widths below and above md
- [x] Visual pass per page (home, conditions, webcams, forecasts, about/sources/explainers) at phone and desktop widths confirms consistent typography and stepped headings

## Comments

- Implemented 2026-09-11. The sweep (recursive grep over all `src/**/*.scss` for `font-size`, `@media`, `line-height`, `font:`, plus TSX `fontSize` inline props) found the contract already holding: the only raw font-size declarations are the nine documented frozen literals (WeatherCurrent 2.3rem/1.6rem/1.1em, AuroraNow Kp readout 3rem, live-panel value family 1.5rem ×2, moon badge 2.7rem, webcam pin 3rem), the only width media query is the `respond-to` mixin itself, and the raw `@media` survivors are the two sanctioned non-width queries (`prefers-reduced-motion` in `index.scss`/`Nav.scss`, `orientation` in `OvalGlow.scss`). Every `@include respond-to` file carries its own `@use "…/breakpoints" as *`. **The checkbox's literal "three literals" is satisfied under the expanded definition**: tickets 01/02 deliberately grew the frozen list (temperature readout, glyphs, tuned line-heights) and ADR-0009 documents the full list; the sweep greps against *that* list, not the spec's original three.
- Swept stragglers (each maintainer-approved): deleted the dead `.pageIntroduction` lead rule (`Pages.scss`, zero TSX consumers — the ticket-01 "maintainer call"); deleted the dead commented-out `@media (prefers-reduced-motion)` block in `AuroraNow.scss` (reduced-motion stays covered by the global kill in `index.scss`); fixed an em dash the en-dash source test caught in a `Nav.scss` comment (Vitest 787/788 RED → GREEN, the ticket's one red→green cycle). Doc drift fixed in the standards doc: the frozen list now includes the Aurora Now readout's 2.5rem line-height (ADR-0009 had it, the standards doc had dropped it), the CollapsiblePanel h3-step is reclassified as a sanctioned shape deviation (it is token-based, not a raw literal), and the contract's stylesheet boundary is stated — Recharts SVG text (tick/legend/tooltip `fontSize` props, which cannot take CSS custom properties) and MUI icon `fontSize` props are chart/icon chrome; `font: inherit`/`font-size: inherit` are resets, not sizes. ADR-0009's consequences gained the same boundary sentence.
- Glossary check: CONTEXT.md's Type token and Breakpoint entries match the shipped system verbatim (roles, values, breakpoints, mixin, mobile-first) — no drift. ADR-0009's frozen list matches the code value-for-value.
- Convention verification is grep-as-acceptance-criteria per the spec's testing decision (maintainer re-confirmed: no automated source-contract test for typography, unlike the colour equivalent `ui-palette-final.test.ts`).
- Verification: typecheck clean; Vitest 788/788 (80 files; the en-dash contract initially failed on the ticket-02 Nav comment and passes after the fix); Playwright fresh-build 65 tests — typography 5/5 (computed-style checks below/above md and the below-sm fold), mobile-layout 9/9, smoke 20/20, webcams 7/7, offline 5/5, product-page and shared-page a11y green; the only failures are the six documented environmental ones (5 conditions + home color-blind toggle, all external-data-dependent), re-verified reproducing identically on clean HEAD via stash; one extra home-a11y failure in the first run was a network flake that passes on re-run. Visual pass: full-page screenshots of all 11 routes (home, conditions, webcams, discussion, 3days, 27days, daily, weekly, about, sources, explainers) at 390/700/1280px — zero horizontal overflow everywhere, headings step with the md breakpoint, panel titles fold below sm, no layout breakage. Code review (two-axis, standards + spec agents): zero hard violations, zero smells; the four cleanups above were pre-approved maintainer calls. Not committed — awaiting maintainer review.
