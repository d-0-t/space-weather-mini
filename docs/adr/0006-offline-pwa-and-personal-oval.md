# 0006: Offline PWA with honest stale data and personal oval (OVATION canvas + view distance)

We add a Service Worker at build time and a personal oval on Home that answers _can I see it from my town_ without lying. The app is still client-side-only per ADR-0001, still `suncalc` on device per ADR-0005, and still polls live JSON per ADR-0003 — this ADR locks how the offline shell, the map, and the distance band work together.

Why: the app currently does not load without internet (`vite.config.ts:1` has no worker). Chasers on the hill need the shell to open offline and the last forecast to stay visible with honest age. They also need an _honest_ local answer: the OVATION `Forecast Time` grid (`services.swpc.noaa.gov/json/ovation_aurora_latest.json:42` ~900 KB, 1° `Observation Time`/`Forecast Time`/`Data Format [Lon,Lat,Aurora]`) gives per-cell forecast energy `0–25` quiet today (46k cells `0` transparent), `≥6` already rare, `≥16` top 2% — not Kp. Kp (`CONTEXT.md:72`) is planetary; `Aurora` is local. A single `240 km` looks like a fact and was `wontfix` in `horizon-1-now-dashboard/issues/03-where-am-i-distance-pill.md:9`; we replace it with a band + confidence.

**Status**: accepted

**Considered Options**:

- No worker / AppCache — rejected, breaks on reload offline.
- `vite-plugin-pwa` `generateSW` with Workbox `StaleWhileRevalidate` for `services.swpc.noaa.gov`, `CacheFirst` for Stadia tiles + OVATION JPGs — picked, static Netlify/gh-pages build stays zero-ops, one `manifest.json:1` `display:standalone` already exists.
- Per-page stale hiding (`Never show stale`) — rejected, chaser would be blind; we keep stale with `As of` + `⚠ Showing saved data`.
- Separate place pickers for Home and `/conditions` — rejected, duplicates `geocoded place` (`CONTEXT.md:167` single `localStorage["sw:local-conditions:place:v1"]`). Reuse one `PlaceFinder` modal next to the short name on both pages, icon-only on narrow with `sr-only`.
- Full pannable world map at zoom 6+ — rejected, burns Stadia tiles (64→256). Locked `minZoom 1` / `maxZoom 2-3` world view (2→4 tiles, horizontal wrap) shows both caps via `North | South` toggle (OVATION has north + south payloads), ~8 tiles total, well within Stadia free.
- Single-number distance (`Aurora 240 km N`) — rejected as misleading — picked band table with confidence, `0` transparent.
- Hatch + contour on default map — rejected for clean default; picked hatch + contour only in color-blind mode. Default stays NOAA-like color wash; color-blind mode swaps palette and adds pattern.

**Consequences**:

- `vite-plugin-pwa` added to `vite.config.ts:1`, `VitePWA({ strategies:'generateSW', registerType:'autoUpdate', workbox:{ globPatterns:['**/*.{js,css,html,woff2}'], runtimeCaching:[{urlPattern:/services\.swpc\.noaa\.gov/, handler:'StaleWhileRevalidate', options:{cacheName:'swpc', expiration:{maxEntries:50, maxAgeSeconds:3600}}}, {urlPattern:/tiles\.stadiamaps\.com/, handler:'CacheFirst', options:{cacheName:'stadia', expiration:{maxEntries:20, maxAgeSeconds:604800}}}, {urlPattern:/services\.swpc\.noaa\.gov\/images\/animations\/ovation/, handler:'CacheFirst', options:{cacheName:'ovation-jpg', expiration:{maxEntries:30, maxAgeSeconds:3600}}}]}, includeAssets:['favicon.ico','assets/*'], manifest:false })` with `skipWaiting` + `clientsClaim` for iOS. Shell precached; first visit must be online; iOS 7-day eviction documented honestly. Offline UI everywhere: `As of {Forecast Time} • Updated {age} • ⚠ Showing saved data — couldn't reach NOAA` when `isError && data` (mirrors `forecasts/3-day-forecast.tsx:106` stale branch), plain error when never loaded.
- One `geocoded place` shared: `Home` `AuroraNow` line `Aurora ~band from [shortName] (i)` + `Update location` button + same modal `PlaceFinder` used on `/conditions` (refactored from section to `location icon+shortName` button). `device location` (`CONTEXT.md:171`) via `getCurrentPosition({enableHighAccuracy:true, timeout:8000, maximumAge:60000})` reverse-geocoded via Nominatim, becomes stored place only on confirm. `±{accuracy}m` shown.
- OVATION canvas: fetch `ovation_aurora_latest.json`, decimate 1° grid, project equirectangular to `<canvas>` over Stadia `alidade_smooth_dark` tiles (locked zoom). `Forecast Time` shown, not `Observation Time`, with 30–90 min lead note. `Aurora 0` transparent. Starting bands `1-5 faint / 6-10 moderate / 11-15 strong / 16+ intense` (calibrated live; `max 25` today, higher in storm) mapped to frozen `.kp01–.kp9` palette reused via `color-mix` (`docs/adr/0002:3` never variable-ized). Default: color wash only. Color-blind mode (`localStorage["sw:oval:cb:v1"]` toggle, `aria-pressed`): same bands remapped to Okabe-Ito/viridis safe ramp + alternating hatch (`\` vs `/` vs `X`, neighbours never share) + thin white contour per band edge. Hatch via `OffscreenCanvas` `CanvasPattern` 16×16 @ 6% opacity. Legend shows swatch + hatch + line style; canvas `role="img"` `aria-label` lists bands; hidden `<table>` is source of truth (`docs/agents/coding-standards.md:26`). Hover/tap tooltip `Aurora 14 ~ Kp 6` optional.
- View distance (`CONTEXT.md` new term): band to nearest cell `Aurora ≥6` within 600 km, haversine ring. Table `Overhead / Nearby ~0-100 km • Likely / Distant ~100-300 km • Possible / Far ~300-600 km • Unlikely` + `Not in range`. `(i)` explainer: `Forecast, not a photo. Each colored square is 30-min forecast (1°). 0 = no forecast. Cloud/moon/town lights can still hide it.` Stored thresholds versioned for migration.
- Light pollution stays link-out per ADR-0005; no Bortle number invented. Webcams aggregation unchanged per ADR-0004.
- Honest copy everywhere: `30-min forecast` provenance `https://www.swpc.noaa.gov/products/aurora-30-minute-forecast` Newell 2009 link in explainer, no promise of background alerts, no periodic background sync (0% iOS `research:185`).

**Implementation note (2026-09-03, ticket 01)**: the SWPC runtime route is registered as `/services\.swpc\.noaa\.gov\/(json|text|products)/` — narrowed to the data paths so Workbox's first-match-wins ordering cannot shadow the OVATION JPG CacheFirst route (all app-fetched SWPC data URLs still match). The offline copy ships with the en dash (`–`) because `src/test/en-dash.test.ts` bans U+2014 in code; the em dash in this ADR is the intent, the en dash is the enforced presentation. Production TanStack Query uses `networkMode: "always"` so offline SPA navigation surfaces the honest error instead of pausing on "Loading…".

**Implementation note (2026-09-04, ticket 04 follow-up)**: the UI dropped the numeric `1–5/6–10/11–15/16+` ranges (same core brightness occurs at any Kp, so ranges read as storm strength); levels stay Faint/Moderate/Strong/Intense as local brightness under the `Oval glow intensity` heading, parser thresholds unchanged for ticket 05. Canvases paint at grid resolution with `image-rendering: pixelated` (no downscale seams).

**Implementation note (2026-09-05, ticket 04 follow-up)**: the tile layer is keyed Stadia `alidade_smooth_dark` at world zoom 1 (2 tiles per cap, `CacheFirst` 7 days, `© Stadia Maps © OpenMapTiles © OpenStreetMap`). Stadia 401s without a key and CARTO watermarks `API KEY REQUIRED` into the pixels (both verified live) – the key arrives via `VITE_STADIA_API_KEY` (local `.env`, deploy env; restrict it to the app domain), and the layer is omitted entirely when unset. A tile-less graticule interval was tried and reverted per review. Gap root cause found with evidence: `Math.floor` on float division dropped 20 grid columns to true 1px holes (live grid verified complete at 65,160 cells) – projection now rounds + clamps with a regression test. Freshness is one line (`Forecast Time {ft} – 30–90 min lead • Updated {age}`, age from `observationTime`) after the date duplicated across two lines.

**Implementation note (2026-09-06, ticket 05)**: the View distance line ships under the oval in `AuroraNow` (`ViewDistanceLine`): `Aurora {band} (i) – {place} [Change location]` – the confidence reads lowercase mid-sentence with no preposition (`from`/`at` do not work for every band; user review), the place is plain text (`shortPlace` short name), and one `btn--secondary` `Change location` button (PlaceFinder `actionLabel`, which turns the trigger into an icon+label action button with no flag/place pill) opens the shared modal. The line is a `<div>`, not a `<p>`: the `(i)` popover's `<details>` is not phrasing content, so a browser would auto-close a `<p>` around it and break the line apart. The `(i)` sits right after the info it explains and its popover is absolutely positioned like the oval-glow family (never an in-flow text block flooding the panel); it carries the approved copy, the band-table rows and the NOAA provenance link (HelpPopover gained an optional `footnote` slot). The helper `distanceToNearestAurora` (`src/products/view-distance.ts`) is table-driven from `VIEW_DISTANCE_BANDS` (the approved 100/300/600 edges are each row's `upToKm`) and excludes the boundary artifact rows through the same `isBoundaryRow` the paint uses (now defined in `products/ovation.ts` so products own the grid rule) – an equator place never reads a phantom band off a model artifact. The threshold is stored versioned `sw:view-distance:threshold:v1` (`{ threshold, v: 1 }`, default 6, clamped 1-25 against the Aurora scale; no threshold UI yet – the key exists so a future chaser-set threshold migrates cleanly). Freshness: the oval's own line shows `Forecast Time {ft} ({local} your time) – 30–90 min lead.` - the UTC time is NOAA's publishing reference, the parenthetical is the device-local `HH:MM` (date included when the zone crosses midnight, `formatLocalShort`) so a near-now forecast time never reads as wrong; the single `As of {Forecast Time}. Updated {age}.` line belongs to the View distance block (user review: the age must appear once, and separators are periods/commas/colons/dashes – never bullets). Per the standing no-sr-only-tables rule (user, re-confirmed 2026-09-06) the oval's sr-only per-hemisphere counts table is gone for good; the counts returned through `countGlowLevels` (boundary rows excluded, numbers matching the painted map) as an on-demand visible disclosure between the lead line and the map – a closed-by-default `<details>` named `Glow intensity table` by its visible text with a decorative `ReadMore` icon, the realized table-alternative pattern of the coding standards. The oval stage keeps the user-restored `rgb(1, 3, 11)` deep-space background with the `#444444` land fill – recorded as a deliberate legibility pick (the oval must read at maximum brightness; the once-claimed 3.66:1 was a miscalculation, real 2.16:1), not a WCAG non-text-contrast claim, and the palette sweep carries the frozen carve-out.
